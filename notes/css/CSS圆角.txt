CSS2：
背景图片实现圆角：
1.宽度固定，高度自适应的圆角，切图时切两个图：
需要4个block标签，使用圆角矩形容器设定固定宽度，同圆角宽度，用背景图片实现顶部圆角
div>top_radius->content->bottom_radius

2.宽高自适应，切图时切四个角，用相对定位和绝对定位实现，四个角全部绝对定位

标签模拟圆角：
使用像素画中的弧线绘制圆角

CSS3：
border-radius,值表示圆角半径
