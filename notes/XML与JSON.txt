XML可扩展标记语言，用来标记数据，定义数据类型，使用DTD定义文档类型，可跨平台
JSON是一种轻量级的数据交换格式

XML
优点：格式统一，符合标准；容易与其他系统交互；
缺点：文件格式负责，文件庞大，占用传输带宽，解析花费时间；解析XML的代码较复杂，不容易维护

JSON
优点：易于读写，体积小，浏览器解析快；代码容易解析；十分稳定
缺点：没有XML那么通用

JavaScript处理json:
JSON.stringify方法用于将一个值转为字符串。该字符串符合JSON格式，并且可以被JSON.parse方法还原。