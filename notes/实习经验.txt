总体架构：
前台放less样式，和与ajax有关的coffee文件

后台放各个路由，经过路由解析后渲染页面，页面中的js，css都是前台编译过来的


性能优化方面：
刚开始去的时候，网站访问量只有1,2万，到后来突破十万，服务器很快扛不住了，扛不住体现在访问的时候非常卡，图片也上传不上去，当时图片还没有分离出来，于是就靠一两个工程师彻夜加班，将原来的功能修改，并运用了七牛服务器存储图片

对PC端频繁访问的rabbitpre页面，所有图片都必须经过压缩，加载顺序也要经过考虑

服务器读写分离，加上memcache缓存

数据库mongo加上索引后快了很多
